#ifndef SPLIT_SERVICE_HPP
#define SPLIT_SERVICE_HPP

#include <bits/stdc++.h>
#include <string>
#include <vector>
#include <iostream>

class SplitService 
{
    public:
        static std::vector<int> split(std::string line, char separator);
};

#endif